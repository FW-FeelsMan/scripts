
//новый код
static string[] ResizeArray(string[] array, int newSize) //Ёто объ€вление функции ResizeArray, котора€ принимает два аргумента: массив строк array и новый размер newSize. ќна будет возвращать новый массив строк.
{
    string[] newArray = new string[newSize];// «десь мы создаем новый массив строк newArray с размером, указанным в newSize

    //  опирование элементов из старого массива в новый
    for (int i = 0; i < Math.Min(array.Length, newSize); i++)//ћы начинаем цикл, который будет копировать элементы из старого массива в новый. 
    //ћы используем Math.Min, чтобы определить, сколько элементов нужно скопировать - это минимум между текущей длиной старого массива и новым размером.
    {
        newArray[i] = array[i];//¬ этой строке мы копируем элемент из старого массива с индексом i в новый массив newArray с тем же индексом i. 
        //Ёто происходит внутри цикла дл€ каждого элемента
    }

    return newArray;//ну а тут мы возвращаем это говно
}